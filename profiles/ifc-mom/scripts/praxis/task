#!/usr/bin/env bash
set -euo pipefail

if ! command -v task >/dev/null 2>&1; then
  if ! command -v brew >/dev/null 2>&1; then
    echo "go-task is not installed and brew is unavailable." >&2
    exit 1
  fi
  brew install go-task/tap/go-task
fi

cd "$(dirname "$0")/../.."
exec task "$@"
