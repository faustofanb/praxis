schema_version: 1
id: M0-T004
title: establish CI and clean-clone reproducibility proof
milestone: M0
status: accepted
risk_class: B
objective: "Produce the M0 scenario and failure acceptance evidence: a clean clone of the accepted M0 commit bootstraps via mise and passes check:all, and injected lint/type errors actually fail their gates. Record the evidence in docs/acceptance/M0.md without flipping its Exit Decision."
evidence: 
  observed: 
    - CI workflow (.github/workflows/ci.yml) runs frozen install plus check:all on every push/PR
    - no clean-clone execution has been recorded as an acceptance artifact yet
  expected: 
    - "docs/03 M0 exit criteria: a fresh clone reproduces the environment with one bootstrap flow and mise run check:all passes"
    - "docs/05 failure acceptance: injected lint/type errors must make the gates FAIL"
  references: 
    - docs/03-project-plan.md
    - docs/05-acceptance-strategy.md
    - docs/acceptance/M0.md
    - .praxis/milestones/M0.yaml
hypothesis: "Cloning the accepted M0 commit to a temporary directory and running the documented bootstrap (mise trust, mise install, mise run check:all) reproduces the environment and passes every gate; injecting a type error and a lint error into the clone makes typecheck and lint fail respectively."
scope: 
  allowed_paths: 
    - .praxis/tasks/M0-T004.yaml
    - .praxis/state.yaml
    - docs/acceptance/M0.md
    - .praxis/handoffs/**
  forbidden_paths: 
    - packages/**
    - apps/**
    - scripts/**
    - mise.toml
    - package.json
architecture_refs: 
  - docs/07-architecture-conformance.md
required_gates: 
  []
acceptance: 
  scenarios: 
    - clean clone of the accepted M0 commit bootstraps and passes mise run check:all
  failure_cases: 
    - an injected type error makes mise run typecheck fail in the clone
    - an injected lint error makes mise run lint fail in the clone
  artifacts: 
    - docs/acceptance/M0.md evidence section
    - .praxis/handoffs/ generated by ai:handoff
falsified_if: "The clean clone cannot bootstrap with the documented flow, check:all fails on the clone, or an injected lint/type error does not fail its gate."
requires_adr: false
requires_human_acceptance: false