# 验收结论

## 1、验收范围

技能发现跟随目录符号链接。

## 2、功能验收

- `_installed_skills` 改用 os.walk(followlinks=True)，符号链接技能可被发现。
- doctor 实测：installed 56→64，policy_without_provider 5→0。

## 3、异常场景验收

- 符号链接环防护：seen 集合按真实路径去重，环不会导致无限递归（测试覆盖）。

## 4、数据校验

- 内容哈希对符号链接技能正确（SHA-256 与真实目录一致，测试断言）。

## 5、构建结果

`uv run --extra dev python -m py_compile src/praxis/skills/routing.py` exit 0。

## 6、测试结果

`uv run --extra dev pytest tests/test_skill_discovery.py -q --no-cov`：2 passed
（符号链接发现 + 环防护）。

## 7、类型与代码质量检查

未执行（需独立授权）。

## 8、最终结论

验收通过。符号链接技能发现已修复并实测验证，环防护有测试保障。
