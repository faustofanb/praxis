# 实施计划

## 一、总体目标

`_installed_skills` 遍历跟随目录符号链接，使符号链接安装的技能可被发现。

## 二、范围说明

- `src/praxis/skills/routing.py` `_installed_skills`：用跟随符号链接的递归遍历替换 rglob，
  加环防护（seen 集合）。
- `tests/test_skill_discovery.py`（或并入现有测试）：符号链接技能被发现 + 环防护。

## 三、验证矩阵

| 项目 | 分类 | 精确命令 | 授权 | 证据 |
|---|---|---|---|---|
| 聚焦测试：符号链接技能被发现 | 可自动化 | `uv run --extra dev pytest tests/test_skill_discovery.py -q --no-cov` | 待执行 | |
| 聚焦测试：符号链接环不崩溃 | 可自动化 | 同上 | 待执行 | |
| 最小编译 | 可自动化 | `uv run --extra dev python -m py_compile src/praxis/skills/routing.py` | 待执行 | |
| doctor 实测 | 可自动化 | `praxis doctor --json`（验证 policy_without_provider 减少） | 待执行 | |
