# 验收结论

## 1、验收范围

画像增强（模块结构/规则摘要/真实命令/子仓库技能）与命令检测优化。

## 2、功能验收

- **命令检测**：mom/aotu web 从 `npm run build` 修正为 `pnpm build`/`pnpm build:antd`，
  typecheck 检出 `pnpm check:type`；mom backend 检出 24 个 pom 模块；
  aotu web CLAUDE.md（1337 行）规则摘要与命令块正确提取。
- **子仓库技能**：mom 发现 5 个（fullstack-development、validate-die-location、
  3 个 web 技能），aotu 发现 9 个（ifc-* 系列）；全部登记到 knowledge/skills/business
  且路由可识别（skill list/search 实测可见），无 skill.toml 时自动补最小清单。
- **路由可见性**：mom business 11→16，aotu business 7→16。

## 3、异常场景验收

- 无 CLAUDE.md 的仓库规则摘要显示"（无规则文件）"。
- 子仓库技能只有标准 SKILL.md（无 skill.toml）时自动生成最小注册清单，路由可解析。

## 4、构建结果

`uv run --extra dev python -m py_compile src/praxis/portraits/service.py` exit 0。

## 5、测试结果

`uv run --extra dev pytest tests/test_portrait_enhanced.py tests/test_portraits_integrations.py -q --no-cov`：
12 passed（4 新增 + 8 既有）。

## 6、类型与代码质量检查

未执行（需独立授权）。

## 7、最终结论

验收通过。mom/aotu 画像已增强（真实命令、模块结构、规则摘要、子仓库技能），
命令检测正确性与技能可发现性经实测验证。
