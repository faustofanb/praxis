# 实施计划

## 一、总体目标

增强 PortraitService 画像扫描（模块结构/关键目录/规则摘要/真实命令），发现并登记子仓库
技能到 knowledge/skills，命令提取支持 praxis.toml 显式配置优先。

## 二、范围说明

### 1. 画像增强（portraits/service.py）

- `_commands` 重构：
  - 优先 `project.lint_commands/typecheck_commands/test_commands`（praxis.toml 配置）
  - 无配置时读 package.json scripts：语义匹配 build*/test*/lint*/check:type 等，pnpm 优先
    （packageManager 含 pnpm 时命令前缀 pnpm）
  - pom.xml 存在时读 modules 输出构建命令（`mvn -pl <module> -am clean install -DskipTests`
    作为模块级命令）
  - CLAUDE.md/AGENTS.md 存在时提取 ```bash 代码块的 mvn/pnpm/npm 命令作为补充
- 新增画像字段：`module_structure`（pom modules / 关键目录）、`rule_summary`
  （CLAUDE.md/AGENTS.md 概述前 3 行 + 命令块摘要）、`subrepo_skills`（发现的技能列表）
- `_write` 渲染新增字段

### 2. 子仓库技能发现（skills/registry.py 或新函数）

- `discover_subrepo_skills(root, project)`：扫描各子仓库 `skills/`、`docs/skills/`、
  `.cursor/skills/` 目录下的 SKILL.md
- 登记到 `knowledge/skills/business/<system>.<repo>.<skill>/`（复用现有业务技能布局），
  id 命名 `business.<system>.<repo>.<skill-id>` 防重名
- 幂等：已登记且内容哈希一致时跳过

### 3. CLI 接线

- `praxis portrait scan --project <id>` 已有；新增 `praxis system scan --project <id>
  --include-subrepo-skills` 可选开关（默认开）

## 三、不在本次范围内的内容

- 不修改 mom/aotu 任何子仓库内部代码
- 不处理 Gap 2（委派子会话 hook schema）
- 不重写画像存储结构（兼容既有 portrait JSON）

## 四、阶段划分

1. TDD RED：画像命令检测（pnpm/模块/CLAUDE.md 提取）+ 子仓库技能发现测试
2. TDD GREEN：实现
3. 最小编译
4. mom/aotu 实测扫描：`praxis --root <dir> portrait scan --project <id>` 验证画像输出
5. 提交合并

## 五、验证矩阵

| 项目 | 分类 | 精确命令 | 授权 | 证据 |
|---|---|---|---|---|
| 聚焦测试：pnpm 命令检测 | 可自动化 | `uv run --extra dev pytest tests/test_portrait_enhanced.py -q --no-cov` | 待执行 | |
| 聚焦测试：pom 模块结构 | 可自动化 | 同上 | 待执行 | |
| 聚焦测试：子仓库技能发现与登记 | 可自动化 | 同上 | 待执行 | |
| 最小编译 | 可自动化 | `uv run --extra dev python -m py_compile src/praxis/portraits/service.py src/praxis/skills/registry.py` | 待执行 | |
| mom/aotu 实测 | 可自动化 | `praxis --root <mom\|aotu> portrait scan --project <id>` | 待执行 | |
| 完整回归、lint、typecheck | 需独立授权 | 未获批不执行 | | |
