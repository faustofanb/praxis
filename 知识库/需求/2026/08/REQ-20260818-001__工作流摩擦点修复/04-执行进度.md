# 执行进度

## 总体进度

需求状态：实施完成，进入验证（focused 测试与 CLI 验收复演通过；完整回归/质量复核按授权边界未执行，见 06-验收结论）。

## 当前阶段

验证（implemented → verifying 推进中）。

## 已完成事项

1. **调查分析（investigating/analyzed/planned/ready）**：对 5 个摩擦点逐一对照代码与历史会话证据复核现状（含推翻了“未提及项目 recorded_at 会被刷新”的子假设，storage 循环只触碰被提及项目；已加回归守卫测试）。
2. **工作树**：`worktree preview` + `worktree ensure --confirm` 创建绑定工作树
   `.worktrees/REQ-20260818-001__工作流摩擦点修复/REQ-20260818-001__工作流摩擦点修复__praxis-next`，
   binding `WT-REQ-20260818-001--praxis-next`。
3. **TDD RED→GREEN**：新增 5 个测试文件/测试块，全部先观察失败再实现；聚焦套件 41 passed。
4. **摩擦点 1：record-implementation 空列表 fail-closed**：
   - `src/praxis/knowledge/requirements.py` `record_implementation` 新增 `reset` 参数；非 reset 时空列表且项目已有产出物时返回
     `REQUIREMENT_IMPLEMENTATION_RESET_REQUIRED`（列出已有 ids，提示加 `--reset` 或传完整列表）；
   - CLI `--reset` 标志、application 分派、errors 目录新条目；
   - 未提及项目（含空列表项目之外）的 `recorded_at` 保持不变（回归测试守护）。
5. **摩擦点 2：implemented 单步回开发**：
   - `requirement reopen` 新增 `--from implemented|verifying`；implemented 必须显式 `--from implemented`，否则
     `REQUIREMENT_REOPEN_STATUS_INVALID`，hint 含 `--from implemented` 与 fast 通道 `praxis fix start ... --small`；
   - domain `reopen` 支持 VERIFYING/IMPLEMENTED 来源，storage `reopen_requirement` 按 from_status 校验；
   - errors 目录 `REQUIREMENT_REOPEN_STATUS_INVALID` 补齐单步命令与 fast 指引。
6. **摩擦点 3：测试静默跳过检测**：
   - 新增 `src/praxis/guides/testing.py`：扫描全部 pom.xml 的 `<properties>` 中 surefire/maven.test.skip 默认 true 属性，
     输出含 `-pl` 与 `-D<属性>=false` 的可复制命令（`praxis test-hint --path <仓库> [--module <模块>]`）；
   - 实测 mom develop 分支发现 `lamp-dependencies-parent/pom.xml` 在 surefire 插件配置硬编码 `skipTests/skip=true`
     （`-D` 无法覆盖，属性形态在 `local` 分支），补 `maven_hardcoded_surefire_skips` 检测并在 note 提示参数化或切分支；
   - `guide tdd` 场景首步加“先检测测试跳过属性”；SKILL.md 同步声明两种形态。
7. **摩擦点 4：提交钩子归属声明**：
   - `worktree/service.py` 新增 `detect_commit_hooks`（husky-pre-commit / pre-commit-framework / git-pre-commit / lint-staged），
     `ensure` 输出携带 `commit_hooks` 字段与 note：“提交钩子触发的校验属于提交流程一部分，不视为未授权运行”；
   - SKILL.md 第 4 节声明钩子归属，禁止绕过/卸载钩子。
8. **摩擦点 5：advance 到 implemented 的未登记产出物提示**：
   - `advance` 目标为 implemented 且 binding 存在未登记代码变更时返回 `artifact_hint`
     （`praxis artifact add --requirement <id> --type code-change --source <路径> --stage implemented --binding <binding_id>`）。
9. **本机 matt 技能自动路由**：
   - `node-routing.toml` 追加 5 个 conditional 策略（code-review/implement/diagnosing-bugs/resolving-merge-conflicts/ask-matt），
     全部 `source = "local:agents"`；测试断言策略存在与节点路由命中。
10. **验收复演（CLI 层）**：验收 1-3 全部通过（详见 06-验收结论），聚焦套件 41 passed，compileall ok。

## 进行中事项

- 登记产出物（artifact add）与 `requirement record-implementation`。
- 节点门禁推进（in_progress → implemented → verifying → completed）。
- 工作树提交并合并回模板分支 `codex/praxis-v3-development`；重装全局 praxis；重新生成 mom/aotu guidance。

## 阻塞事项

- 完整回归、lint、typecheck、覆盖率与代码质量复核未获独立授权，不执行（如实记录，不投影为 passed）。

## 最近一次更新

2026-08-18T07:05:00+00:00。
