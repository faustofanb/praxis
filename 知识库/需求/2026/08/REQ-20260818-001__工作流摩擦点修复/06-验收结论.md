# 验收结论

## 1、验收范围

- REQ-20260818-001 五项摩擦点修复（praxis-workflow CLI 与 praxis-requirement-workflow skill）：
  1. record-implementation 空列表覆盖已有产出物（fail-closed）
  2. implemented 状态回开发路径过长（单步 reopen）
  3. 测试被静默跳过无提示（test-hint）
  4. pre-commit 钩子强制 lint 与“未授权不跑 lint”冲突（钩子归属声明）
  5. advance 到 implemented 无未登记产出物提示（artifact_hint）
- 附加：本机 matt 技能自动路由（node-routing.toml 5 条 conditional 策略）。
- 验收方式：TDD 聚焦测试 + CLI 层验收复演 + 真实 mom 仓库 pom 两种形态 smoke。

## 2、功能验收

| 验收项 | 结果 | 证据 |
|---|---|---|
| 空列表清空已有产出物被阻断，列出已有 ids，提示 `--reset` | 通过 | CLI 复演：`REQUIREMENT_IMPLEMENTATION_RESET_REQUIRED`，保留 `['ART-...']`；`--reset` 后 `RECORDED` |
| 未提及项目的 `recorded_at` 不变 | 通过 | 先记录 frontend=ART-9 再仅传 backend，frontend `recorded_at` 逐字节一致 |
| implemented 单步回开发（`--from implemented`） | 通过 | 单测 + CLI 复演；不带 `--from` 时报错且 hint 含完整单步命令 |
| errors 目录 `REQUIREMENT_REOPEN_STATUS_INVALID` 含单步与 fast 指引 | 通过 | `--from implemented` 与 `fix start` 均在 next_step |
| 属性形态 surefire 跳过 → 可复制命令含 `-D` 覆盖与 `-pl` | 通过 | 真实 `local` 分支 pom（`ifc.surefire.skipTests/skip`）：`mvn test -pl lamp-mes-bff/lamp-mes-bff-server -Difc.surefire.skipTests=false -Difc.surefire.skip=false` |
| 硬编码 surefire skip（`-D` 无法覆盖）被检出并提示 | 通过 | 真实 develop 分支 pom：`hardcoded_skips` 列出 `lamp-dependencies-parent/pom.xml#skipTests` 与 `#skip`，命令不含伪 `-D`，note 提示参数化或切分支 |
| worktree ensure 输出携带提交钩子触发项 | 通过 | 单测：husky/pre-commit 框架/git 钩子/lint-staged 检出；`commit_hooks.detected` 含 `husky-pre-commit` |
| advance 到 implemented 输出未登记产出物提示 | 通过 | 单测：binding 有未登记变更时 `artifact_hint` 含 binding_id、unregistered_files 与 `praxis artifact add ...` 完整命令 |
| matt 技能自动路由 | 通过 | `test_matt_skill_routing.py`：5 条策略均存在且 `mode=conditional`，`node="verifying"` 意图命中 `code-review` |

## 3、异常场景验收

| 场景 | 结果 | 证据 |
|---|---|---|
| 对已登记产出物项目传空列表 | 阻断，不写入 | CLI 复演返回 reset 错误，原 ids 保留 |
| 未传 `--from` 对 implemented reopen | 报错 + 指引 | `REQUIREMENT_REOPEN_STATUS_INVALID`，hint 含单步命令 |
| `test-hint --path` 指向不存在目录 | 报错 | `TEST_HINT_REPOSITORY_NOT_FOUND` |
| Maven 显式配置优先于命令行（硬编码 skip） | 不产生无效 `-D` | 命令仅 `mvn test -pl ...`，note 说明无法覆盖 |

## 4、数据校验

- 未提及项目 `recorded_at` 不变（storage 循环只触碰被提及项目），测试 `test_record_implementation_preserves_unmentioned_project_recorded_at` 守护。
- 空列表阻断发生在写入前，已有 `artifact_ids` 原样保留。

## 5、构建结果

- `python -m compileall -q src/praxis`：exit 0。

## 6、测试结果

- 聚焦 TDD 套件：41 passed（`tests/test_workspace_requirements.py tests/test_worktree_commit_hooks.py tests/test_guides.py tests/test_guides_testing.py tests/test_matt_skill_routing.py tests/test_agent_guidance.py`），
  命令 `PYTHONPATH=src <venv>/bin/python -m pytest -q ... -o addopts=''`。
- RED 阶段先确认失败（新增测试在实现前失败）。

## 7、类型与代码质量检查

- **未执行**：完整回归、lint、format、typecheck、覆盖率与代码质量复核需要独立验证授权，本需求未获批，如实记录为未执行（不投影为 passed）。

## 8、影响范围复核

- 涉及文件：`src/praxis/{application,cli,domain/requirement,guides/errors,guides/service,guides/testing(新),knowledge/requirements,storage/sqlite,worktree/service}.py`、
  `skills/praxis-requirement-workflow/SKILL.md`、`skills/praxis-system-development/references/node-routing.toml` 及 5 个测试文件。
- 行为变更点均 fail-closed：空列表阻断、reopen 需要显式来源、test-hint 提示、钩子归属声明、advance 提示，不改变既有成功路径输出。

## 9、性能验证

- 不适用：变更集中在静态扫描与状态写入提示，无热路径；未发现性能风险。

## 10、遗留问题

- 完整回归/lint/typecheck/覆盖率与代码质量复核未执行（未获独立授权）。
- mom 仓库 develop 分支 surefire 硬编码 skip 属上游既有配置，本次仅提示不改动该仓库。

## 11、最终结论

五项摩擦点修复与 matt 技能自动路由全部按验收标准通过（聚焦测试 + CLI 复演 + 真实仓库 smoke）。
完整质量门禁按授权边界如实记录为未执行；建议合入模板分支后由全局安装验证。
