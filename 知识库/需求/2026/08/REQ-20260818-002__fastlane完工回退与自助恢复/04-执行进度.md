# 执行进度

## 总体进度

需求状态：实施完成，验收通过，待合并回模板。

## 当前阶段

验证。

## 已完成事项

- [x] 调查四项优化点现状（02-调查分析.md）
- [x] 实施计划与验证矩阵（03-实施计划.md）
- [x] TDD 实施（RED→GREEN，49 passed）
  - 新增 `tests/test_fastlane_reopen_reuse.py`（8 个测试）：P0 completed 回退（service + CLI `--from completed` 合法）、P1 `--worktree` 复用跳过 create、repository mismatch 拒绝、typecheck 恢复动作 hint、errors 目录全量覆盖 46 码、doctor 警告
  - 聚焦套件：`test_fastlane_reopen_reuse.py test_workspace_requirements.py test_guides.py test_guides_testing.py test_matt_skill_routing.py test_worktree_commit_hooks.py test_agent_guidance.py` → **49 passed**
- [x] minimum-module-compile：`compileall -q src/praxis` 通过
- [x] CLI 验收复演（临时工作区 /tmp/praxis-cli-replay-002）
  - P0：`requirement reopen REQ-... --from completed --reason ...` → `status: in_progress`，审计 `requirement.reopened` details `{from: completed, reason, status}` ✓
  - P1：`fix start --worktree <binding>` 参数解析→应用层 ✓（复用/拒绝逻辑由 service 测试覆盖）
  - P1：`errors FAST_LANE_TYPECHECK_NOT_CONFIGURED` 含 praxis.toml 恢复动作 ✓；`doctor` warnings 含 "仓库 backend（java-maven）缺少 typecheck_commands" ✓
  - P2：`errors SMALL_FIX_WORKTREE_REPOSITORY_MISMATCH` 可查 ✓（全量 46 码由测试覆盖）

## 变更文件（工作树，8 源 + 1 测试）

- src/praxis/fastlane/small_fix.py：`start()` 支持 `worktree_binding` 复用；completed/verifying 自动 reopen 回 in_progress
- src/praxis/domain/requirement.py：reopen 白名单扩展为 verifying/implemented/completed
- src/praxis/knowledge/requirements.py：reopen 支持 from_status=completed，返回 hint 含 `--from completed`
- src/praxis/storage/sqlite.py：reopen 映射接受 completed
- src/praxis/fastlane/service.py：`_typecheck_argv` 返回 `FAST_LANE_TYPECHECK_NOT_CONFIGURED` 带恢复 hint
- src/praxis/application.py：fix.start 透传 worktree_binding；doctor 输出强类型仓库缺 typecheck_commands 警告
- src/praxis/cli/__init__.py：`fix start --worktree` 参数；`requirement reopen --from` choices 增补 completed
- src/praxis/guides/errors.py：补齐 46 个 fast-lane 错误码
- tests/test_fastlane_reopen_reuse.py：新增 8 个测试

## 进行中事项

- 产出物登记（artifact add）、requirement record-implementation
- 门禁推进 implemented → verifying → completed
- 合并回模板分支 codex/praxis-v3-development，推送
- 重装全局 praxis（uv tool install --from . --force），重新生成 mom/aotu guidance

## 阻塞事项

- 完整回归 / lint / typecheck / 覆盖率：按计划未授权，不执行（登记为 declined/未执行，不投影为 passed）

## 最近一次更新

2026-08-18T07:50:00+00:00。
