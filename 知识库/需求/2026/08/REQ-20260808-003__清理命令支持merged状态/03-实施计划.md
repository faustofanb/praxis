# 实施计划

## 一、总体目标

`cleanup_for_requirement` 安全判定从 `!= "active"` 改为 `not in {"active", "merged"}`，
使 merge 后的残留（binding、分支、上下文包）可通过 cleanup 完整清除。

## 二、范围说明

- `src/praxis/worktree/service.py`：安全判定条件改为放行 active + merged。
- `tests/test_worktree_cleanup.py`：新增 merged 状态清理测试。

## 三、验证矩阵

| 项目 | 分类 | 精确命令 | 授权 | 证据 |
|---|---|---|---|---|
| 聚焦测试：merged binding 允许清理 | 可自动化 | `uv run --extra dev pytest tests/test_worktree_cleanup.py -q --no-cov` | 待执行 | |
| 最小编译 | 可自动化 | `uv run --extra dev python -m py_compile src/praxis/worktree/service.py` | 待执行 | |
| dry-run 实测 | 可自动化 | `praxis worktree cleanup REQ-20260808-002 --dry-run --json`（merged binding） | 待执行 | |
