# 验收结论

## 1、验收范围

引导层 Skill 瘦身：将 AGENTS.md/CLAUDE.md 每会话静态注入的约 200 行 managed 块瘦身为「指针 + 不变式 + 表格」（约 55 行），操作细则（fast_fix/RTK/TDD/预算/证据指纹缓存/命令纪律/subagent）去重收敛到内置 `praxis-requirement-workflow` SKILL.md 作为唯一权威源按需加载。

## 2、功能验收

- `_managed_content` 重写为紧凑版：保留工作空间身份、入口解析、需求/工作树不变式、pre-commit guard、TDD 声明、fast_fix 触发识别、codegraph-impact-analysis、DBX `select current_database()`、`requirement advance` 规则、RTK 行、Skill 调用协议与 `--used-skill` 示例，以及仓库模板分支/节点 Skill 策略/项目规则来源三个动态表。
- 迁出的操作细则已在 SKILL.md 中存在（唯一权威源）：`mode=fast_fix`/`tests=declined_by_user`/`compile=not_requested`/`scope=target_file_only`、命令预算与停止条件、证据指纹缓存、禁止为门禁创建实现形状测试、禁止扩大为全仓构建。
- AGENTS.md / CLAUDE.md 由 `workspace guidance` 重新生成，受管块约 55 行正文，net 删减约 145 行（原约 200 行），两客户端内容保持同一受管源，与 SKILL.md 重复消除。
- 行为保证不变：强制门禁（fail-closed 状态流转、worktree 绑定、pre-commit guard）仍在代码侧，未移入 Skill。

## 3、异常场景验收

- 受管 marker 不匹配（`AGENT_GUIDANCE_MARKERS_INVALID`）仍保持阻断，测试覆盖。
- 入口解析（MCP 可用/不可用、CLI fallback.path）与 DBX 只走 MCP 的不变式保留在受管块。
- 用户触发 fast_fix 表达时，受管块仍保留一行识别指引，细则指向 SKILL.md，不丢失行为。

## 4、构建结果

最小 Python 模块编译：

`uv run --no-sync python -m compileall -q src/praxis`，exit code 0。

## 5、测试结果

聚焦测试命令（TDD RED→GREEN）：

`/Users/fausto/plugins/praxis-next/.venv/bin/python -m pytest -q tests/test_agent_guidance.py tests/test_workflow_optimization.py -o addopts=''`。

结果：12 passed。断言拆分后覆盖：AGENTS 指针/不变式/动态表（route-node、complete-node、`select current_database()`、TDD、fast_fix、独立验证授权、rtk、codegraph-impact-analysis、演示工作空间、backend/.cursor/rules、brainstorming（必需）、local/develop），以及 SKILL.md 迁出细则（mode=fast_fix、正则匹配源码禁止、目标文件指纹、禁止扩大为全仓构建）。

## 6、类型与代码质量检查

用户通过 AskUserQuestion 批准「完整回归 + lint/typecheck」验证矩阵：

- 完整回归：310 passed / 5 failed。5 个失败均为既有失败（`test_skill_node_routing.py` 4 项 + `test_mcp_server_unit.py` 1 项，与 tdd/test-driven-development skill 解析有关），已在主工作区 HEAD 同参数验证同基线，非本变更引入。
- ty typecheck：改动文件 0 诊断（`src/praxis/agents/guidance.py`、`tests/test_agent_guidance.py`、`tests/test_workflow_optimization.py` 唯一诊断为环境 unresolved-import pytest，位于未改动的 test_workflow_optimization.py 第 6 行）。
- ruff：改动文件全部通过（全仓 7 项 E501/F401 均在未改动文件，主工作区 HEAD 同基线）。

未批准项如实登记 decline receipt，不投影为通过：

- 真实 Codex/Claude CLI smoke：未获单独批准，未执行（VDR-20260817T034140-7A107F2E）。
- 覆盖率达标（cov-fail-under=85）：完整回归附带输出 57.65%，未达阈值且不在批准矩阵内（VDR-20260817T034140-5F231FDD）。

## 7、影响范围复核

代码变更产出物 `ART-20260817T033454-E1908975` 已登记，绑定 `WT-REQ-20260817-001--praxis-next`。变更文件：`src/praxis/agents/guidance.py`（唯一业务代码改动）、`tests/test_agent_guidance.py`（断言拆分）、`AGENTS.md`/`CLAUDE.md`（受管重新生成）；SKILL.md 核对后确认已含全部迁出细则，无需改动。TDD 与验证 approval receipts 已登记（APR-20260817T034105-C907FC4B、APR-20260817T034105-0071E47C）。

## 8、性能验证

未执行独立性能基准；本变更目标是每会话静态 token 约减 3/4，属上下文体积优化，未改变数据库查询、锁、事务或并发模型。受管块由约 200 行降至约 55 行。

## 9、人工验收授权

用户已通过 AskUserQuestion 明确批准验证矩阵（完整回归 + lint/typecheck）与未批准项的 decline 登记；本需求不需要额外交互式人工验收。

## 10、遗留问题

真实 Codex/Claude CLI smoke、覆盖率达标和独立代码质量复核未执行，已登记 decline receipt，保留为后续验证项。

## 11、最终结论

验收通过。已登记的紧凑版受管内容、拆分断言后的聚焦测试（12 passed）、最小模块编译（exit 0）以及已批准验证（完整回归 310 passed/5 既有失败、ty 0 诊断、ruff 清零）满足本需求交付门禁；未批准验证项如实登记，未投影为通过。需求可进入 completed。
