# 调查分析

## 1、需求目标

确认 Codex 已安装 `praxis-next`，且当前仓库初始化后可通过统一 CLI 返回 Praxis V3 版本。

## 2、业务背景

## 3、当前实现

项目在 `pyproject.toml` 中注册 `praxis = "praxis.cli:main"`，版本常量为 `3.0.0`；Codex 插件通过 `.codex-plugin/plugin.json` 和 `.mcp.json` 暴露技能与 MCP Server。

## 4、涉及业务域

`plugin-development`（插件开发）。

## 5、涉及系统和仓库

系统与仓库均为 `praxis-next`，仓库路径为当前目录。

## 6、涉及模块

## 7、业务规则

## 8、数据口径

## 9、接口与数据流

## 10、现有能力复用

直接复用项目已有 `uv run praxis version`，无需增加测试脚本或依赖。

## 11、代码图谱调查

## 12、数据库调查

## 13、影响范围

仅新增 Praxis 工作空间事实、需求知识资产和运行状态，不修改产品逻辑。

## 14、风险分析

插件清单版本包含 Codex 缓存戳，而 CLI 产品版本保持 `3.0.0`；两者用途不同，不应混淆。

## 15、待确认事项

## 16、调查证据

- `codex plugin list` 显示 `praxis-next@personal` 已安装并启用。
- `praxis.toml` 已登记系统、业务域、仓库和验证命令。

## 17、分析结论

现有版本命令足以完成最小验收，执行一次命令并将结果注册为测试报告产出物即可闭环。
