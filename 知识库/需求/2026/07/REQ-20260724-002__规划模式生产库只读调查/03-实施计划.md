# 实施计划

> 执行方式：当前会话内联执行。每项修改遵循 RED → GREEN → 最小重构。

## 一、总体目标

让已登记的生产 DBX 连接在规划模式可执行有目的、只读、非持久化调查，同时维持所有写入
和目标识别安全边界。

## 二、范围说明

- 调整 `DatabaseService.investigate()` 的生产连接判定。
- 更新 Agent guidance、需求工作流 Skill、数据库调查 Skill。
- 更新数据库服务与 guidance 聚焦测试。

## 三、不在本次范围内的内容

- 不连接或查询实际生产数据库。
- 不改变 `database query` 的生产写入门禁。
- 不新增确认参数、连接类型或持久化调查记录。

## 四、阶段划分

### 任务 1：确认调用链和安全边界

涉及文件：

- `src/praxis/database/service.py`
- `src/praxis/application.py`
- `src/praxis/cli/__init__.py`
- `tests/test_database.py`

步骤：

- [ ] 创建并绑定需求隔离工作树。
- [ ] 对 binding 执行 CodeGraph `ensure-fresh`。
- [ ] 查询 `database.investigate` 到 `DatabaseService.investigate` 的调用路径和 Blast Radius。
- [ ] 确认 `DatabaseService.query()` 的生产写入阻断不在修改范围。

### 任务 2：以 TDD 修正运行时门禁

修改文件：

- `tests/test_database.py`
- `src/praxis/database/service.py`

步骤：

- [ ] 把生产调查断言改为：已登记生产连接的 `select` 成功，执行数据库身份核验后执行查询。
- [ ] 保留同一测试中的 write SQL、空 purpose 拒绝断言，并补充未登记连接或锁定读取回归。
- [ ] 运行
  `rtk test env PYTHONPATH=src python -m pytest -q tests/test_database.py -k plan_investigation --no-cov`，
  预期因 `DATABASE_PRODUCTION_INVESTIGATION_BLOCKED` 失败。
- [ ] 删除 `DatabaseService.investigate()` 中仅针对生产连接的拒绝分支。
- [ ] 重跑同一命令，预期全部通过。

### 任务 3：同步公开规则表述

修改文件：

- `src/praxis/agents/guidance.py`
- `skills/praxis-requirement-workflow/SKILL.md`
- `skills/dbx-database-investigation/SKILL.md`
- `tests/test_agent_guidance.py`

步骤：

- [ ] 先修改 guidance 测试，要求明确包含“已登记生产连接可只读调查”及禁止项。
- [ ] 运行
  `rtk test env PYTHONPATH=src python -m pytest -q tests/test_agent_guidance.py --no-cov`，
  观察旧文案导致的 RED。
- [ ] 同步 Agent guidance 与两个 Skill 文档，不修改历史计划记录。
- [ ] 重跑数据库与 guidance 聚焦测试，预期通过。

### 任务 4：最小验证与交付记录

步骤：

- [ ] 运行聚焦测试：
  `rtk test env PYTHONPATH=src python -m pytest -q tests/test_database.py tests/test_agent_guidance.py --no-cov`。
- [ ] 运行最小编译：
  `rtk err env UV_CACHE_DIR=/tmp/praxis-next-uv-cache uv run --no-sync python -m compileall -q src/praxis`。
- [ ] 运行 `rtk git diff --check`。
- [ ] 更新 CodeGraph 索引并执行 `affected` 复核。
- [ ] 登记代码产出物和 implementation；不自动运行完整测试、lint、typecheck、reviewer、提交或推送。

## 五、阶段依赖

首次代码编辑依赖工作树 active、TDD 授权收据和新鲜 CodeGraph Blast Radius。

## 六、风险与回退方案

风险集中在数据库访问边界。回退时恢复生产调查阻断及对应文案；任何非只读、未登记连接、
目标库不匹配或生产写入测试失败都不得交付。
