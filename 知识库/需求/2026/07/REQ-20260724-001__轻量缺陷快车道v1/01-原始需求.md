# 原始需求

## 首次提出

### 提出时间

2026-07-24T08:51:40.420760+00:00

### 用户原文

> 实现轻量缺陷快车道 v1：新增 praxis fast start|confirm|red|status|finish，复用现有需求、工作树、批准、产出物和 Skill 服务。仅适用于单仓、1–3 个已跟踪业务文件，排除数据库、API 契约、权限、事务和并发等风险；越界时保留需求与工作树并降级标准流程。start 登记候选需求、启动 5 分钟定位预算并返回工作树 preview；confirm 校验资格、幂等生成轻量调查/计划、创建工作树，一次生成 development_tdd 与 verification 收据，并在未修改工作树懒生成类型诊断基线；red 保存真实 TDD RED；status 返回只读状态；finish 校验范围后执行 GREEN、git diff --check 与基线增量类型检查，登记产出物和 implementation，但不提交、推送、运行全量构建/测试/reviewer 或推进 completed。FastLaneService 使用 runtime_state 保存 candidate→confirmed→red_recorded→implemented|downgraded，记录 5/10/2 分钟软预算。类型基线缓存键包含项目、模板 SHA、命令 argv、锁文件与工具链指纹，规范化 TypeScript/ty/mypy 诊断并输出 passed、incremental_passed_baseline_failed、failed_new_diagnostics、baseline_unavailable；基线不可用允许 implemented 但验证待定。命令使用 argv：测试走 rtk test、差异走 rtk git diff --check、类型检查走 rtk proxy，RTK 不可用不静默直跑。新增 fast-defect-v1 Skill profile，只审计 praxis-requirement-workflow、systematic-debugging、test-driven-development；file-search、ponytail、karpathy-guidelines 作为 execution_principles。轻量文档 managed block 幂等更新，并提供 Vitest + ESM 的 MES PAD 纯函数测试模板。v1 只新增 CLI 与内部 fast.* application operations，不新增 MCP。按 TDD 新增 tests/test_fast_lane.py、tests/test_fast_lane_diagnostics.py，并更新既有接口和路由测试。聚焦验证命令：rtk test uv run --no-sync pytest -q tests/test_fast_lane.py tests/test_fast_lane_diagnostics.py tests/test_skill_node_routing.py tests/test_interfaces.py --no-cov；随后执行最小 Python compileall。完整 pytest、Ruff、ty、代码审查、提交和推送不在授权范围。

## 后续补充
