# 实施计划

## 范围

- 新增 `SmallFixService` 与 `fix.start|finish` application operations。
- 扩展 WorktreeService 支持固定模板 commit 创建。
- 新增 `small-fix-v2` Skill profile。
- 完善 code-change 对 small-fix 显式未跟踪文件的归档。
- 增加服务、生命周期、CLI、profile 与兼容测试和 README。

## TDD 任务

- [x] RED：SmallFixService、CLI/profile 和固定 revision 接口缺失。
- [x] GREEN：start 复用需求、固定 SHA 工作树、范围/基线类型模式。
- [x] GREEN：finish 资格检查、聚焦测试、diff-check、类型检查和单一产出物。
- [x] GREEN：81 行降级、verifying reopen、历史诊断不阻断。
- [x] 回归：fast v1、标准 profile、标准 WorktreeService 行为不变。

## 限定验证

```text
rtk test env PYTHONPATH=src /Users/fausto/plugins/praxis-next/.venv/bin/python \
  -m pytest -q tests/test_small_fix.py tests/test_fast_lane.py \
  tests/test_fast_lane_diagnostics.py tests/test_lifecycle.py \
  tests/test_skill_node_routing.py tests/test_interfaces.py --no-cov

rtk err env PYTHONDONTWRITEBYTECODE=1 \
  /Users/fausto/plugins/praxis-next/.venv/bin/python -m compileall -q src/praxis

rtk git diff --check
```

不运行完整 pytest、Ruff、ty、全量构建、reviewer 或发布校验。
