# 变更记录

- 新增 `praxis fix start|finish`。
- 新增 `SmallFixService` 和 `small-fix-v2` profile。
- WorktreeService 新增固定模板 revision 创建路径。
- code-change 支持 small-fix 显式纳入未跟踪变更文件。
- README 增加命令、资格与验证说明。
- 失败重试复用已有精确审批收据；范围类型检查失败使用专用结果码。
- 固定模板 revision 记录 `worktree.template_revision_selected`，不再误记为模板同步。
