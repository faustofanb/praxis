# 执行进度

## 总体进度

需求状态：第六轮工作树依赖准备已实现；当前 MOM Web 真实准备因业务锁文件缺陷 fail-closed。

## 当前阶段

提交 Praxis 更新并刷新 Codex 插件缓存。

## 已完成事项

- 工作树开发分支统一为 `praxis/<需求ID>`。
- 需求工作空间调整为 `.worktrees/<需求ID>__<简称>/<repository_id>`。
- 新增稳定的需求-仓库绑定 ID，并兼容旧分支键绑定。
- 修复 Skill 自定义 outcome 污染完成状态的问题，并兼容已有 `completed_at` 记录。
- 调查节点增加必需的 `grilling` 路由，明确 `grill-me` 仅为用户入口。
- 将节点 Skill 默认预算从 2000 提升到 4000，覆盖调查阶段全部必需 Skill。
- 更新 Codex/Claude Code 管理规则、聚焦测试契约和使用文档。
- 完成 Python 语法编译、TOML 解析和 `git diff --check` 静态校验。
- 将 CodeGraph 初始化纳入工作树 active 不变量，失败 binding 保留为 blocked，可重复 create 自愈。
- CodeGraph 状态支持 binding 与仓库工作树路径查询。
- Skill gate 成功和失败均产生独立审计；stage 可省略并缺省为 development。
- 业务 Skill 按 project ID 过滤；Java 规范改为仓库类型和代码修改意图同时命中。
- 状态投影保留已有执行进度；已绑定工作树显示 bound_active 并保留 Worktrunk 原始状态。
- 聚焦缺陷测试 9 项通过；受影响模块回归测试 76 项通过。
- 确认 MOM 遗留 CodeGraph 索引实际已完整构建，但调用方中断导致 binding 永久 initializing。
- 新增 CodeGraph 持久操作记录、心跳、成功/失败/中断收口与已完成索引安全收养。
- CodeGraph 活动心跳或 lock 存在时返回 busy；worktree binding 保持 initializing，不误标 blocked。
- 第三轮新增关键路径测试 5 项通过，CodeGraph、worktree、CLI/MCP 受影响回归 70 项通过。
- 第三轮 Skill 路由、6 项调用完成凭证和节点门禁均已落审计；门禁审计为
  `AUD-20260721T062243-F1A63E4E`。
- 已清理第三轮 MOM/AOTU 验证工作树及精确对应的本地测试分支，其他业务工作树未动。
- 缺少当前节点 Skill route 时，状态迁移和工作树创建统一 fail-closed，不再静默放行。
- `SKILL_ROUTE_NOT_FOUND` 等门禁失败现在同样写入 `skill.gate` 审计并返回 `audit_id`。
- 工作树删除后通过 Git 实际分支列表核验 Worktrunk 声明；声明已删但分支仍在时返回
  `WORKTREE_BRANCH_DELETE_MISMATCH`，不再记录虚假的完整成功。
- 第四轮新增关键缺陷测试先按预期失败，修复后 4 项关键测试及 76 项受影响回归通过。
- 第四轮插件已安装为 `3.0.0+codex.20260721065551`。
- MOM 新需求 `REQ-20260721-006` 在 captured 状态迁移和 ready 工作树创建两处缺路由场景均被拦截并返回独立审计；正向创建后 binding active、Git 干净且 CodeGraph fresh。
- AOTU 新需求 `REQ-20260721-003` 完成相同负向/正向验证；固定模板保持 `auto/local`，发布分支 `origin/auto/test` 为需求分支祖先，CodeGraph fresh。
- 两个新验证需求均维护了调查分析、实施计划和执行进度；新工作树保留供用户检查。
- 第五轮增加项目级 `local_files` 白名单：配置加载拒绝越界路径，binding 激活前从主仓库
  安全复制 ignored 本地运行文件，失败保持 blocked 且不启动 CodeGraph。
- MOM/AOTU 的 Web 项目均已登记 `apps/web-antd/.env.development`；MOM 当前
  `REQ-20260721-003` Web 工作树已补齐该文件，内容一致、权限保持且仍被 Git 忽略。
- 第六轮已实现 `worktree_setup_commands` 配置与 local-files → setup → CodeGraph 的激活顺序；
  setup 失败保持 blocked，审计不记录进程输出，相同 active 配置不重复执行。
- MOM/AOTU 所有带 pnpm 锁文件的前端仓库均已登记离线冻结锁文件安装命令。
- 当前 MOM Web 实际准备返回 `WORKTREE_SETUP_FAILED` 并将 binding 标记 blocked，审计号
  `AUD-20260721T080316-07AA4C25`；进一步离线诊断确认锁文件缺少 vue-draggable-plus 快照。
- 第六轮实现节点 Skill 门禁通过，审计号 `AUD-20260721T080152-9A4A8281`；未调用测试或
  质量复核 Skill。

## 进行中事项

完成第六轮插件刷新与当前 MOM Web 工作树离线依赖初始化；测试执行仍等待用户明确批准。

## 阻塞事项

未运行 lint、typecheck、全量测试、业务项目测试或代码质量复核。

新暴露的非阻塞问题：

- Skill 路由对否定语义不敏感，`不修改业务代码` 仍会条件命中 `file-search`/`karpathy-guidelines`。
- 状态投影为保护自定义内容不再覆盖 `执行进度.md`，但也不会自动追加最新状态；需要显式 `requirement progress`，否则页面仍显示初始 captured。
- CodeGraph 虽有持久 heartbeat，CLI 在约 2–3 分钟初始化期间没有进度输出，用户侧仍像卡住。
- ready 门禁把空模板归入 `missing_documents`，诊断名未区分“文件不存在”和“文件存在但内容不完整”。
- 第五轮 `Testing Writing Guidelines` 路由为 `blocked_pending_approval`；未新增或执行测试、
  lint、类型检查、覆盖率和质量复核，仅执行配置加载、白名单准备与文件一致性安全门禁。
- 第六轮 `Testing Writing Guidelines` 仍为 `blocked_pending_approval`；只允许配置验证与当前
  MOM Web 的离线依赖准备，不执行任何项目质量命令。
- MOM Web `pnpm-lock.yaml` 当前无法通过冻结安装；修复必须作为业务仓库变更单独批准，Praxis
  不使用 `--no-frozen-lockfile` 自动改写。

## 最近一次更新

2026-07-21。

- 2026-07-21T14:02:47.242503+08:00：第二轮真实重跑暴露新缺陷：MOM CodeGraph 初始化产生大型数据库后命令无 Result，binding 停留 initializing，fresh=false 且无完成/失败审计；已按 fail-fast 停止 AOTU。下一轮需设计超时、原子完成协议、陈旧 initializing 恢复和外部进程诊断。
- 2026-07-21T14:22:43+08:00：第三轮完成根因确认与恢复协议实现。健康索引将被验证后收养，活动初始化保持 busy；70 条受影响回归通过，等待发布后真实重跑。

- 2026-07-21T14:32:07.547429+08:00：第三轮恢复协议已发布为 3.0.0+codex.20260721062433；MOM 自愈为 active/fresh，AOTU 首次初始化完成且 operation 记录 25 次心跳，两边 Git 分支、模板 revision、知识文档均符合预期。新 P0 缺陷：两个需求均在没有 ready 节点 Skill route/gate 的情况下进入 ready 并创建 active 工作树，补跑门禁均返回 SKILL_ROUTE_NOT_FOUND。下一轮应把节点 route/gate 原子接入状态迁移，并在 worktree create 校验 ready gate 凭证。

- 2026-07-21T14:33:22.147366+08:00：门禁缺陷补充证据：MOM/AOTU 补跑 ready gate 均返回 SKILL_ROUTE_NOT_FOUND 后，audit_events 中仍无对应 skill.gate 记录，说明缺路由分支在统一审计逻辑之前提前返回。下一轮除原子接入状态迁移和 worktree create 外，还必须保证 SKILL_ROUTE_NOT_FOUND 等所有早退分支写失败审计并返回 audit_id。

- 2026-07-21T14:55:00+08:00：第四轮完成 fail-closed 门禁与删除后 Git 核验。旧 MOM/AOTU 验证工作树和分支已清理；关键缺陷测试完成红绿验证，受影响回归 76 项通过，等待技能门禁、提交和插件刷新。

- 2026-07-21T15:16:08+08:00：第四轮真实重跑完成。MOM/AOTU 的 captured 状态迁移和 ready 工作树创建均在缺 route 时 fail-closed 并产生 audit_id；完整 Skill 凭证后两边工作树均 active/fresh，模板与需求分支干净。记录四项 P1：否定语义误路由、执行进度不自动追加、CodeGraph CLI 无实时进度、空模板诊断命名不精确。

- 2026-07-21T15:24:00+08:00：第五轮完成本地运行文件准备实现。根因确认为 Git worktree 不复制 ignored 文件；采用 `local_files` 精确白名单从 `project.path` 主仓库复制。MOM/AOTU Web 配置已登记，当前 MOM Web 工作树已补齐并通过内容一致、权限和 Git ignore 门禁；测试执行未获批准，保持未执行。

- 2026-07-21T16:03:00+08:00：第六轮确认 node_modules 不随 Git worktree 共享；采用显式离线 setup 命令在 CodeGraph 前完成依赖准备，MOM/AOTU pnpm 前端配置已更新。测试和质量复核继续等待用户明确批准。

- 2026-07-21T16:04:00+08:00：当前 MOM Web 真实离线准备 fail-closed，binding 标记 blocked。兼容 pnpm 版本与 autoInstallPeers 设置后仍确认锁文件缺少 vue-draggable-plus 快照；未联网、未改写锁文件、未执行测试。
