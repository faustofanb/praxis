# 执行进度

## 总体进度

需求状态：第二轮优化实现完成，等待安装后重跑 MOM/AOTU 验证。

## 当前阶段

Praxis 插件更新与验证。

## 已完成事项

- 工作树开发分支统一为 `praxis/<需求ID>`。
- 需求工作空间调整为 `.worktrees/<需求ID>__<简称>/<repository_id>`。
- 新增稳定的需求-仓库绑定 ID，并兼容旧分支键绑定。
- 修复 Skill 自定义 outcome 污染完成状态的问题，并兼容已有 `completed_at` 记录。
- 调查节点增加必需的 `grilling` 路由，明确 `grill-me` 仅为用户入口。
- 将节点 Skill 默认预算从 2000 提升到 4000，覆盖调查阶段全部必需 Skill。
- 更新 Codex/Claude Code 管理规则、聚焦测试契约和使用文档。
- 完成 Python 语法编译、TOML 解析和 `git diff --check` 静态校验。
- 将 CodeGraph 初始化纳入工作树 active 不变量，失败 binding 保留为 blocked，可重复 create 自愈。
- CodeGraph 状态支持 binding 与仓库工作树路径查询。
- Skill gate 成功和失败均产生独立审计；stage 可省略并缺省为 development。
- 业务 Skill 按 project ID 过滤；Java 规范改为仓库类型和代码修改意图同时命中。
- 状态投影保留已有执行进度；已绑定工作树显示 bound_active 并保留 Worktrunk 原始状态。
- 聚焦缺陷测试 9 项通过；受影响模块回归测试 76 项通过。

## 进行中事项

更新缓存标识、提交、重装插件，然后重跑两个已保留验证需求。

## 阻塞事项

未运行 lint、typecheck、全量测试、业务项目测试或代码质量复核。

## 最近一次更新

2026-07-21。
