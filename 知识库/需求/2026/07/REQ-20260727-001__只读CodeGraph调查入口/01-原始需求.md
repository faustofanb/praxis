# 原始需求

## 首次提出

### 提出时间

2026-07-27T03:29:50.269604+00:00

### 用户原文

> 是，部分属于工作流覆盖问题，也有我当时判断偏保守的问题。
> 不是 CodeGraph 不可用：ifc-mes-pad 和 ifc-mom-column-max 都有有效的 .codegraph 索引。
> 没有调用的直接原因是：
> 当前规则允许“普通调查先用 rg”，CodeGraph 被定义为高风险改动编辑前的强制证据。
> 本次处于 Plan Mode，只做诊断，没有登记需求、创建工作树或准备修改代码。
> codegraph-impact-analysis 要求从需求 worktree binding 获取索引并核对 fingerprint；当前没有 binding，所以无法完整执行该 Skill 的审计协议。
> 数据库记录和操作日志已直接证明“4 次保存对应 4 次换模”，所以我继续使用了 rg + DBX。
> 但这里确实存在两个工作流问题：
> 路由配置不一致
> AGENTS.md 的 investigating 节点没有列出 CodeGraph；但实际路由配置允许在 investigating 节点按“跨模块、公共接口、影响范围”等条件触发 CodeGraph。这次正好横跨 MES PAD、BFF、MES Service 和数据库，理论上符合“跨模块”触发条件。
>
> Plan Mode 缺少只读 CodeGraph 通道
> 数据库调查有无需需求编号、persisted:false 的 Plan Mode 入口；CodeGraph Skill 却强依赖需求 worktree binding。虽然可以直接查询主仓索引，但无法形成符合 Praxis 协议的正式调用凭证。
>
> 因此更准确的结论是：
> 不是索引或工具故障。
> 本次数据库结论仍有操作日志支撑，不因没用 CodeGraph 而失效。
> 对这种跨前端、BFF、Service、数据库的根因调查，应该补充 CodeGraph 调用链证据；我当时只按“只读普通调查”处理，属于可改进的执行判断。
> Praxis 应补一个 Plan Mode 的只读 CodeGraph 调查入口，并统一 AGENTS.md 与真实路由配置。
>
> 用户追加：修复

## 后续补充
