# 验收结论

## 1、验收范围

本次仅执行用户已授权的聚焦 TDD、最小编译、插件校验、提交推送和安装冒烟，不包含完整回归与正式质量复核。

## 2、功能验收

- Service 只读调查入口、CLI 子命令、应用分发和 Agent guidance 的聚焦用例通过。
- 安装缓存中的 `praxis codegraph investigate --help` 可正常展示必填参数。

## 3、异常场景验收

- 聚焦用例覆盖未初始化索引、索引不完整、工作树错配与待同步变更拒绝路径。
- CLI/应用层拒绝 Plan Mode 调查携带 binding/worktree selector。

## 4、数据校验

返回范围标记 `mode=planning_read_only`、`persisted=false`，不写需求绑定和 StateStore。

## 5、构建结果

`python -m compileall -q src/praxis` 通过；personal 安装缓存按 `uv.lock` 重建成功。

## 6、测试结果

- `tests/test_codegraph.py -k plan_mode_investigation`：2 passed。
- `tests/test_interfaces.py tests/test_application_dispatch.py -k codegraph_investigate`：2 passed。
- `tests/test_agent_guidance.py`：2 passed。

## 7、类型与代码质量检查

完整 Ruff、类型检查、覆盖率和正式代码质量复核未获独立授权，未执行。

## 8、影响范围复核

编辑前已取得 CodeGraph 调用链和 Blast Radius 证据；编辑后 affected 遍历 74 个依赖，未返回受影响测试。

## 9、性能验证

未涉及性能目标，未执行专项性能验证。

## 10、遗留问题

完整验证仍待独立授权；当前需求状态保持“已实施”，不标记为完整验收完成。

## 11、最终结论

已完成提交、推送和安装，聚焦验收与安装冒烟通过；因完整验证未授权，本结论为“实施交付完成，完整验收待授权”。
