# M8 Acceptance Contract

> 详细验收制度以 `docs/05-acceptance-strategy.md` 为规范；机器合同见 `.praxis/milestones/M8.yaml`。

## Goal

Produce v1 release candidate with engineering, capability, human UX and recovery acceptance evidence.

## Machine Gates

- [ ] `format`
- [ ] `lint`
- [ ] `typecheck`
- [ ] `unit`
- [ ] `integration`
- [ ] `property`
- [ ] `replay`
- [ ] `fault`
- [ ] `security`
- [ ] `build`
- [ ] `knip`

## Scenario Acceptance

- [x] clean install -> read task -> write task -> approval -> resume/fork —— 于仓库 `/tmp/praxis-accept` 干净克隆（HEAD `1df3b31`，仅含已提交状态）执行（2026-08-30）：`mise run install` frozen 装包 320 个；read 任务（`--script` 确定性脚本）turn completed、11 条 durable 事件逐条流式输出；write 任务无旗标 → durable `ToolRejected`（"capability fs.write requires human approval…(fail closed)"）且文件未出现；`--allow-write` 后同一任务 ToolAuthorized → ToolSucceeded、文件落盘；随后 `run --session` 续跑 open turn（含 §17 恢复编排）。命令与输出即 `docs/13-cli-quickstart.md`（逐字复现）。
- [x] kill process -> resume -> finish —— 同克隆内：`run` 进程 SIGINT（kill -INT）→ 退出码 2、turn 保持 open（cancelled：尝试记为 durable 事实）；用剩余脚本 `run --session <id>` 续跑 → `[485] TurnCompleted` + 最终答案、退出码 0。过程另记录了 loop guard 真实行为：26 脚本序列触发 `turn exceeded 16 model steps` 守卫暂停（exit 2）——多次 resume 每次获得 fresh 16 步预算直至脚本耗尽完成；并确认 `--script` provider 每进程从头消费，kill 后 resume 须提供"剩余脚本"（quickstart 已写明该语义）。

## Failure Acceptance

- [x] release blocker regression suite —— 同克隆内两项注入（docs/05 负向证明规则），注入后均回滚、克隆恢复干净：
  1. 架构违规：向 `packages/store-sqlite/src/index.ts` 注入 `import { foldSessionEvents } from "@praxis/core"`（adapter → core 方向违反）→ `mise run test:architecture` **exit 1**，报 `import-direction: packages/store-sqlite/src/index.ts: store-sqlite -> core is not allowed by architecture.yaml`；
  2. 精确锁违规：将 `packages/provider-openai/package.json` 的 zod 依赖改为 `^4.4.3` → `bunx vitest run tests/boundaries.test.ts` **1 failed**（"all dependencies across every manifest use exact pinned versions"）。
  另有持续性负向证据：core 覆盖地板咬合实验（M7-T010：不可能门槛下 502 测试全过仍 exit 1）与 fault/security 套件（27+40 项持续注入）。

## Human Demo

- [ ] Required

## Real-model Eval

- release eval required —— **已完成**（M8-T004，2026-08-30）：冻结矩阵（v3）在 RC HEAD 重跑全部五配置（n=1/模型×场景，两网关，思考强度经 M7-T011 透传）。发布记录：**gpt-5.6-sol 7/8、gpt-5.6-luna 7/8 并列第一且跨时间点稳定**（`results/eval-2026-08-30T12-39-09-312Z.md` / `results/eval-2026-08-30T12-50-41-711Z.md`）；GLM-5.3 5/8（`results/eval-2026-08-30T12-47-59-241Z.md`）；deepseek-v4-flash 默认 4/8、xhigh 4/8（完成偏置跨时间点复现，`results/eval-2026-08-30T12-32-45-814Z.md` / `results/eval-2026-08-30T12-35-32-807Z.md`）——不宜作无人监督自主代理。完整发布记录（含与 v3 段的方差对照）见 `evals/development-models/BASELINE.md` "v1 release baseline" 节；网关 URL 已脱敏，凭据零入档。

## Evidence Artifacts

- [ ] Machine-readable gate summary —— 各任务 handoff（`.praxis/handoffs/M8-T001…T005-*.md`）随任务累积；里程碑级汇总在全部 M8 任务验收后于此勾选
- [x] Scenario report —— 上文 Scenario Acceptance 节（干净克隆 HEAD `1df3b31`，2026-08-30）
- [x] Failure/fault report where required —— 上文 Failure Acceptance 节（两项注入 + 回滚记录）
- [ ] Human demo record where required —— 待人工演示后记录
- [x] Real-model eval summary where required —— 上方 Real-model Eval 节 + `evals/development-models/BASELINE.md` "v1 release baseline" 节（RC HEAD 五配置，10 份 scorecard 存档）

## Exit Decision

- Result: `PENDING`
- Accepted by: `TBD`
- Evidence reference: `TBD`
