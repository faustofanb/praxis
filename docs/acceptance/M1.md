# M1 Acceptance Contract

> 详细验收制度以 `docs/05-acceptance-strategy.md` 为规范；机器合同见 `.praxis/milestones/M1.yaml`。

## Goal

Implement deterministic Session/Event kernel without a real LLM.

## Machine Gates

- [x] `format` — `mise run check` green at M1-T004 verification (2026-08-28)
- [x] `lint` — biome, same run
- [x] `typecheck` — `tsc -b` project graph, same run
- [x] `unit` — 87 vitest tests (contracts/reducer/store-control-plane)
- [x] `property` — fast-check suites for contracts laws and reducer model agreement (`mise run test:property`)
- [x] `replay` — fixture + checkpoint/restart laws (`mise run test:replay`)
- [x] `integration` — reducer over EventStore port (`mise run test:integration`)
- [x] `build` — `tsc -b` workspace build
- [x] `store` — SQLite adapter suite under Bun (`mise run test:store`, 12 tests incl. parity)

## Scenario Acceptance

- [x] same event stream yields same state in memory and SQLite — property suite `tests/store/store-parity.bun.test.ts`: property-generated streams appended in varying batches to both stores, readStream output and folded state deep-equal; plus deterministic replay of the committed fixture `tests/fixtures/replay/session-lifecycle-v1.json`

## Failure Acceptance

- [x] illegal state transition rejected — `tests/reducer.test.ts` per-transition cases; `tests/property/reducer.property.test.ts` proves any single-seq perturbation or adjacent swap of a valid stream throws `IllegalTransitionError`
- [x] unknown/old schema handled explicitly — union rejects out-of-vocabulary event types (`ToolSucceeded`, `Teleported` cases); SQLite `readStream` revalidates rows so corrupted actor JSON or unknown types fail parsing instead of coercing; the v1 fixture pins historical loadability

## Human Demo

- [x] Not required

## Real-model Eval

- none

## Evidence Artifacts

- [x] Machine-readable gate summary — `.praxis/state.yaml` last_verification (M1-T004 PASS: format/lint/typecheck/unit/property/replay/integration/build/store)
- [x] Scenario report — parity + replay suites cited above
- [x] Failure/fault report where required — failure suites cited above
- [x] Human demo record where required — n/a
- [x] Real-model eval summary where required — n/a
- Task handoffs: `.praxis/handoffs/M1-T001..T004-*.md`; ADR-0009; subsystem docs `docs/subsystems/{contracts,core,store-sqlite}.md`

## Exit Decision

- Result: `ACCEPTED`
- Accepted by: 樊彪（人工，2026-08-28 会话签核）
- Evidence reference: 本文档各节 + `.praxis/state.yaml` verification 记录
