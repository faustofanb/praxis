# M5 Acceptance Contract

> 详细验收制度以 `docs/05-acceptance-strategy.md` 为规范；机器合同见 `.praxis/milestones/M5.yaml`。

## Goal

Harden recovery, context bounds, compaction invariants and session evolution.

## Machine Gates

验收基线 commit `d05d405`（M5 最后一个任务 M5-T004 的 close-out）；9 门于该树逐一实跑，全 PASS，名称与 `.praxis/quality-gates.yaml` 注册表逐一对上：

- [x] `format` — `mise run format:check` PASS（biome ci，142 文件）
- [x] `lint` — `mise run lint` PASS（仅存量 2 warnings + 2 infos，exit 0）
- [x] `typecheck` — `mise run typecheck` PASS（tsc -b，strict + exactOptionalPropertyTypes）
- [x] `unit` — `mise run test:unit` PASS（vitest 47 文件 428/428）
- [x] `integration` — `mise run test:integration` PASS
- [x] `property` — `mise run test:property` PASS
- [x] `replay` — `mise run test:replay` PASS（19/19，清单法 + 双向完备 + 再生 pin）
- [x] `fault` — `mise run test:fault` PASS（15/15，含 M5-T004 崩溃矩阵 8 格）
- [x] `build` — `mise run build` PASS（tsc -b）

附加（合同外超额证据）：`mise run test:store` PASS（13/13，含 SQLite 关闭/重开持久性恢复）。

## Scenario Acceptance

- [x] 1000-turn synthetic session stays bounded
  - `tests/context-compaction.test.ts` "1000 turn" 格：1001 事件流经 fold + projectConversation + buildContext 后恰 1+64 条消息、token 估算 ≤ `maxEstimatedTokens`、`## Compacted history` recap 计数精确（937 user, 0 assistant, 0 tool results）、窗口起点/尾消息步号正确、双构建 `toEqual` 确定性（M5-T002，commit `0739c25`）。
  - `tests/integration/epistemic-context.integration.test.ts` runTurn 集成格：1000-turn 种子后请求恰 65 条消息，系统片段含 goal 与 recap，事件库恰 1001 TurnStarted（M5-T002）。
  - 验收时新鲜实跑：4 个场景套件 40/40（context-compaction 4 + epistemic-context 7 + replay 19 + contracts-replay 10）。
- [x] old fixtures replay after migration
  - `tests/fixtures/replay/index.json` 清单为集合唯一权威（6 fixtures）；`tests/replay/replay.test.ts` 逐条经 `parseReplayStream` 缝隙加载、折叠到记录终态、headSeq 相符、双折叠一致、清单↔目录双向完备、493 事件回归会话规范化 JSON 再生 pin（M5-T003，commit `1a1f5e3`）。
  - `tests/contracts-replay.test.ts` 迁移证据：连续步进迁移表合法性 + 管线盖章版本 + 合成 v1→v2 drill 迁移后折叠到与未迁移流完全相同的 derived state + 未来版本流（schemaVersion 2）加载即拒（`FutureSchemaVersionError`），绝不进 reducer。

## Failure Acceptance

- [x] crash at each critical tool/event boundary
  - `tests/fault/crash-matrix.fault.test.ts`（M5-T004，commit `7a78d01`）：docs/03 M5.3 五边界逐格注入，每格四法则——前缀合法折叠、恢复事实诚实（pre-execute → 显式 `ToolRejected`；跨副作用 → `ToolIndeterminate`→reconcile）、危险工具 execute 计数不越过崩溃前值（无重复 side effect）、恢复幂等（后续 turn 零恢复事实）。皇冠格：结果在手落盘崩溃 → indeterminate-then-reconciled，未验证的 `ToolSucceeded` 被断言不得出现。另含混合 dangling 插入序 + §17 第 9 步人工解锁环全流程。
  - `tests/store/crash-recovery.bun.test.ts`（commit `dc9afbf`）：真实 SQLite 库带 dangling EXECUTING 关闭、同文件重开、前缀逐字节回放、§17 步骤 1-6 走完，side effect 跨进程边界恰好一次。
  - can-fail 证据（验收时实做，scratch 文件未提交、跑完即删）：注入伪证断言"恢复会重执行写工具（executes===2）"→ `mise run test:fault` exit 1（FALSIFY 格失败，1 failed）；删除后 exit 0（15/15）。门可失败，执法真实。
- [x] compaction must retain structured invariants
  - `tests/context-epistemic.test.ts` M5-T001 格：不可压缩层（goal/plan/challenge/pending-indeterminate）永不被字节压力逐出、hypothesis flood 不逐出任何不可压缩分节、病态可压缩分节整节让位、不可压缩层独自放不下抛 `ContextBudgetExceededError`（fail closed）。
  - `tests/context-compaction.test.ts`：recap 计数必须与实际丢弃前缀逐角色精确相符、零丢弃逐字节等同 M5-T001 投影（诚实性 + 无回归）、event store 永不被触碰（forbidden 钉死）。
  - can-fail 证据（验收时实做）：注入伪证断言"1000-turn 构建超出 token 上限"→ `mise run test:unit` exit 1（FALSIFY 格失败）；删除后 exit 0（428/428）。

## Human Demo

- [ ] Required — **PENDING**：待人工执行（建议演示路径：`mise run test:fault` 看崩溃矩阵全绿 → `mise run test:store` 看关闭/重开恢复 → `bunx vitest run tests/context-compaction.test.ts` 看 1000-turn 有界性；或 CLI 起一个会话演示 pause/resume）。本记录不代填。

## Real-model Eval

- eval continues — M5 未新增 eval 场景（本里程碑为恢复/边界/压缩硬化，无新模型行为契约）；M4 的 `evals/development-models/` 套件随 unit 门持续通过（21 项在 428 内），non-core-gating 声明不变。

## Evidence Artifacts

- [x] Machine-readable gate summary — 本文档 Machine Gates 节（9 门 @ `d05d405` 实跑全 PASS）；逐任务机器摘要：`.praxis/handoffs/M5-T002-1788008954287.md`、`.praxis/handoffs/M5-T003-1788011041902.md`、`.praxis/handoffs/M5-T004-1788014001112.md`
- [x] Scenario report — Scenario Acceptance 节（新鲜实跑 40/40 + 各格钉死的性质）
- [x] Failure/fault report — Failure Acceptance 节（矩阵/持久性格 + 两例 can-fail 伪证实做记录）
- [ ] Human demo record where required — PENDING（人工）
- [x] Real-model eval summary where required — Real-model Eval 节（continues，无新增）

## 设计—实现一致性（docs/07 §6）

- 是否新增 package？否——M5 四个任务全部落在既有包（core/contracts/testkit 依赖 + tests/）。
- 是否新增 Core dependency？否——M5 零生产依赖变更；唯工具链改动为 biome `files.includes` 增补 `!!docs/product`（M5-T004 修订记录在合同 scope_notes：外来未跟踪 HTML 草稿污染 lint 门的 130 个错误，与 git 层 exclude 一致的本地排除，无已提交文件受影响）。
- 是否新增 durable contract？否——事件词汇表零变更（schemaVersion 保持 1，迁移表保持空）；M5-T003 新增的是 schema 消费侧缝隙（版本窗口 fail-closed + 步进迁移管线），首次真实 bump 的程序已钉死（drill 证明折叠恒等）。
- 是否让 optional extension 承担 Core invariant？否——压缩 recap 在 core `buildContext` 内确定性计算；恢复法则在 core `run-turn`/`recovery`；矩阵测试只执法不承担。M5-T004 结论：零生产代码改动即全部落格。
- 是否产生两个事实源或两个状态管理器？否——context 是有界投影（每次从事件流重折），store 是唯一历史；fixtures 清单（index.json）是回放集合唯一权威（M5-T003）。
- 是否有系统设计已过期但本次没有更新？否——docs/02 §12.3/§12.4（压缩）、§6.1（版本窗口与迁移法则）、§17（崩溃矩阵表）与 docs/03 M5.1-M5.3 均随任务同步；M5.4 fork 三格保持未勾并附推迟决定（见下节）。

## 推迟记录：M5-T005（session fork，optional）

- 决定：**明确推迟**（非静默跳过）。backlog `optional: true` 且标题条件为 "if evidence shows acceptable complexity"。
- 证据：fork 需要新的持久事件词汇（fork 溯源事实）→ contracts schema 变更 → 按 M5-T003 刚立的版本窗口法则须 `EVENT_SCHEMA_VERSION` 升级 + 首个真实迁移步 + 全部 fixture 重验 + ADR（词汇变更触发 docs/07 §4 停止条件）+ reducer 状态机新法则。这是 M4 以来最大的词汇变更，却服务于一个 optional 便利特性，且在里程碑尾部触发迁移机制的首次实战。
- 时序合法：docs/02 §15 定位 fork 于 "v1 后半段实现"（未指定 M5）；M5 场景/失败验收项均不引用 fork；required_deliverables 的 "session fork/resume" 中 resume 半已交付并被钉死（pause/resume + reconciliation + §17 第 9 步人工解锁环，M5-T004）。
- 复评时点：session evolution 工作重启时（M6+ 或 v1 后半段任一时点），以本记录为起点重估成本；届时按 M5-T003 钉死的迁移程序执行首个真实 schema bump。

## Exit Decision

- Result: `PENDING`
- Accepted by: `TBD`
- Evidence reference: `TBD`（人工评审本文档各节 + handoffs 机器摘要后填写）
