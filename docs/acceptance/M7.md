# M7 Acceptance Contract

> 详细验收制度以 `docs/05-acceptance-strategy.md` 为规范；机器合同见 `.praxis/milestones/M7.yaml`。

## Goal

Quality hardening: fault, security, soak, performance and dependency/maintenance gates for release readiness.

## Machine Gates

全部于验收就绪 commit `da4c80c`（2026-08-30）复跑，证据如下：

- [x] `format`、`lint`、`typecheck` —— `mise run check:all` exit 0（含于同一运行）
- [x] `unit` —— 59 文件 **502/502**（含 10k soak、eval 矩阵 31 项、清单漂移守卫 3 项、praxis-dev 13 项）
- [x] `integration` —— 8 文件 **32/32**
- [x] `property` —— 5 文件 **23/23**（fast-check 状态机硬化 M7-T001）
- [x] `replay` —— **19/19**
- [x] `fault` —— 6 文件 **27/27**（含 provider 逃逸法则 M7-T002、§17 崩溃矩阵 M5-T004）
- [x] `security` —— 6 文件 **40/40**（含 §18 密钥禁锢 M7-T002）
- [x] `build` —— `tsc -b` exit 0
- [x] `knip` —— 零输出零退出（M7-T006 清零后保持）
- 附加：`test:store` 13/13、`test:cli` 11/11（bun 运行时套件）；`test:coverage` exit 0——Core 95/90 地板生效（M7-T010，实测基线 97.58/94.31）

## Scenario Acceptance

- [x] long-run no unbounded queues/state —— M7-T003 soak：10,000 turn / 59,227 事件合成会话，五检查点 context 恒 1+64 消息、token ≤32k、注册表恰好线性、每事件折叠成本 tripwire ≤10×（实测 ~4.4×）；增长模型报告 `docs/10-memory-context-growth.md`（M7-T007）
- [x] full recovery matrix —— docs/02 §17 六格崩溃矩阵逐格注入（`tests/fault/crash-matrix.fault.test.ts` 8 项）+ 真实 SQLite 跨进程持久性（`tests/store/crash-recovery.bun.test.ts`）+ provider 逃逸后诚实失败端到端（M7-T002 `tests/fault/provider-adapter.fault.test.ts` 5 项）；恢复策略文档 `docs/11-sqlite-corruption-recovery.md`（M7-T008）

## Failure Acceptance

- [x] all P0 release blockers actively tested —— 已知 P0：provider 逃逸后重放流致 durable 事实翻倍/幽灵 tool call（M7-T002 修复 + 逃逸法则入 docs/02 §10）；密钥泄漏面（§18 禁锢测试：Authorization 头唯一在途位置，wire body/durable 事件/模型消息全图零泄漏）；capability 绕过组合（`tests/security/bypass-composition.security.test.ts` 等 6 套件 40 项）。无未测试的已知 P0/P1。

## Human Demo

- [ ] Required —— **待人工执行**。建议演示脚本（全部可复现）：
  1. `mise run check:all` —— 全绿（约 2 分钟，含 coverage 地板）
  2. `bunx vitest run tests/soak/` —— 10k soak 四项断言 + 增长分析输出
  3. `bunx vitest run tests/fault/ tests/security/` —— 27 + 40 项故障/对抗用例
  4. （可选）真实模型 eval 基线：`OPENAI_API_KEY=... bun evals/development-models/run-eval.ts` —— 8 场景 scorecard 落 `evals/development-models/results/`

## Real-model Eval

- formal model eval matrix —— M7-T005：8 行正式矩阵（4 行 M4 负向路径 + 4 行 M7-T005 补齐：healthy-plan / completion-legal 正向对照、resolved-indeterminate §8.3 对照、plan-challenge §14），每行 `law` 钉死，7 词决策词表全覆盖（测试断言双向钉死）；矩阵/协议/判分/基线记录家：`evals/development-models/BASELINE.md`。真实模型跑分为操作员动作（M4 验收先例，non-core-gating）——**真实基线数字尚无**，执行后按 BASELINE.md 程序登记。

## Evidence Artifacts

- [x] Machine-readable gate summary —— 十个任务 handoff（`.praxis/handoffs/M7-T001…T010-*.md`）；本节机器门数字 @ `da4c80c`
- [x] Scenario report —— 上文 Scenario Acceptance 各条（vitest/mise 命令可复现）
- [x] Failure/fault report where required —— fault 27 项 + security 40 项套件；P0 修复证据链见 `.praxis/tasks/M7-T002.yaml`（含复现脚本路径与端到端断言）
- [ ] Human demo record where required —— 待人工演示后记录
- [x] Real-model eval summary where required —— `evals/development-models/BASELINE.md`（矩阵 + 基线状态；真实跑分待操作员执行）

## Exit Decision

- Result: `PENDING`
- Accepted by: `TBD`
- Evidence reference: 本文件；实现方最后一个控制面 commit `da4c80c`（M7 backlog T001–T010 全部 accepted，docs/03 M7 质量门 10/10 行勾选）
