# M0 Acceptance Contract

> 详细验收制度以 `docs/05-acceptance-strategy.md` 为规范；机器合同见 `.praxis/milestones/M0.yaml`。

## Goal

Create a reproducible repository foundation with frozen toolchain, workspace boundaries, CI and the AI development control plane itself operational.

## Machine Gates

- [x] `format` — PASS, `ai:verify` at commit `cddaa00`
- [x] `lint` — PASS, `ai:verify` at commit `cddaa00`
- [x] `typecheck` — PASS, `ai:verify` at commit `cddaa00`
- [x] `unit` — PASS, `ai:verify` at commit `cddaa00`（4 files / 31 tests）
- [x] `build` — PASS, `ai:verify` at commit `cddaa00`（`tsc -b` workspace graph）
- [x] `knip` — PASS, `ai:verify` at commit `cddaa00`

（额外：`test:architecture` 门已建立并纳入 `check:all`，同 commit PASS。）

## Scenario Acceptance

- [x] clean clone/bootstrap/check-all — clone of commit `cddaa00`：
  `git clone` → `mise trust` → `mise run install`（frozen，320 packages）→
  `mise run check:all` 全绿（4 test files / 31 tests，coverage 通过，
  `ARCHITECTURE_PASS 7 package(s)`）。

## Failure Acceptance

- [x] lint error must fail gate — 在克隆中注入
  `export function lintFailure() { debugger; }`（`tests/failure-injection.ts`）：
  `mise run lint` 报 `lint/suspicious/noDebugger`，Found 1 error，非零退出。
- [x] type error must fail gate — 注入
  `export const typeFailure: number = "not a number";`：
  `mise run typecheck` 报 `error TS2322`，`exited with code 1`。

## Human Demo

- [x] Not required

## Real-model Eval

- none

## Evidence Artifacts

- [x] Machine-readable gate summary — `.praxis/state.yaml` `last_verification`（M0-T003，7 gates PASS）
- [x] Scenario report — 本文件 + `.praxis/handoffs/M0-T004-*.md`（clean-clone 命令与输出摘要，基准 commit `cddaa00`）
- [x] Failure/fault report — 本文件 Failure Acceptance 节（注入内容与门行为）
- [x] Human demo record where required — 不适用
- [x] Real-model eval summary where required — 不适用

## Exit Decision

- Result: `ACCEPTED`
- Accepted by: `樊彪`（人工，2026-08-27 会话签核）
- Evidence reference: `.praxis/state.yaml` `last_verification`；`.praxis/handoffs/`；commit `cddaa00`；clean-clone 实验（本文件 Scenario/Failure 节）

> 依据 `docs/05-acceptance-strategy.md` §3.2，Milestone 推广需人工或独立评审签核；
> 实现方 AI 将状态推进至 acceptance-ready，Exit Decision 由签核者填写。
