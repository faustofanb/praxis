description = "检查 Tolaria 元数据缺口"
prompt = "使用中文运行文档 Tolaria 元数据缺口检查。优先使用 `rtk task docs -- tolaria-check {{args}}`，不可用时使用 `task docs -- tolaria-check {{args}}`。未提供参数时使用 `--all`。总结扫描数量、问题数量和前几个问题路径；不要发布或重写文档。"
