// Fixture shim: the in-memory EventStore moved to @praxis/testkit
// (M4-T005); existing suites keep their import paths unchanged.
export { inMemoryEventStore } from "@praxis/testkit/in-memory-event-store";
